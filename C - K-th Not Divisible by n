#include <iostream>
using namespace std;

int main(){
    int t; cin >> t;
    while(t--){
        int n,k; cin >> n >> k;
        int d = k/(n-1);
        int r = k%(n-1);
        int res = n * d + r;
        if(r == 0){
            res--;
        }
        cout << res << endl;
    }
}
